services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: wiseflow
      POSTGRES_PASSWORD: wiseflow
      POSTGRES_DB: wiseflow
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
